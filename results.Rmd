---
title: "results"
author: "Nick Hagar"
date: "2/12/2020"
output: html_document
---

```{r setup, include=FALSE}
knitr::opts_chunk$set(echo = TRUE)
library(drake)
library(tidyverse)
```

## Story count changes (overall)
```{r}
loadd(data_noseason_stories)
loadd(rdd_model_data_noseason_stories)
plot(rdd_model_data_noseason_stories, xlab="Days before/after layoffs", ylab="Stories published")
summary(rdd_model_data_noseason_stories)
```

## Byline count changes (overall)
```{r}
loadd(data_noseason_bylines)
loadd(rdd_model_data_noseason_bylines)
plot(rdd_model_data_noseason_bylines)
summary(rdd_model_data_noseason_bylines)
```

## Storyper count changes (cohort)
```{r}
loadd(cohort_agg_storiesper)
loadd(cohort_rdd_cohort_agg_storiesper)
#plot(cohort_rdd_cohort_agg_storiesper)
summary(cohort_rdd_cohort_agg_storiesper)
```

## Tweet embed changes (cohort)
```{r}
loadd(cohort_agg_pct_tweet)
loadd(cohort_rdd_cohort_agg_pct_tweet)
#plot(cohort_rdd_cohort_agg_pct_tweet)
summary(cohort_rdd_cohort_agg_pct_tweet)
```

## Insta embed changes (cohort)
```{r}
loadd(cohort_agg_pct_insta)
loadd(cohort_rdd_cohort_agg_pct_insta)
#plot(cohort_rdd_cohort_agg_pct_insta)
summary(cohort_rdd_cohort_agg_pct_insta)
```

## Char count changes (cohort)
```{r}
loadd(cohort_agg_log_len_chars)
loadd(cohort_rdd_cohort_agg_log_len_chars)
#plot(cohort_rdd_cohort_agg_log_len_chars)
summary(cohort_rdd_cohort_agg_log_len_chars)
```

## Word count changes (cohort)
```{r}
loadd(cohort_agg_log_len_words)
loadd(cohort_rdd_cohort_agg_log_len_words)
#plot(cohort_rdd_cohort_agg_log_len_words)
summary(cohort_rdd_cohort_agg_log_len_words)
```

## Section composition
```{r}
loadd(plot_sections)
plot_sections
ggsave("C:\\Users\\nrh146\\Dropbox\\Apps\\Overleaf\\News layoffs and output - IC2S2\\figures\\section_composition.png", width=4, height=3)
```

